# local mise file with some env variables
mise.local.toml

# output of secrets
secrets/

# files with terraform variables (secrets)
*.tfvars

# files generated by terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
